import { UserCollectionData } from "../model";

class UserClass {
  user: UserCollectionData;
  constructor(
    user: UserCollectionData,
    ) {
      this.user = user;
  }
}
export default UserClass;