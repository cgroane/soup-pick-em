import { useMemo } from "react";
import { UserCollectionData } from "../model";

export const useGetWinPercentage = (user: UserCollectionData) => {
    
    const winPercentage = useMemo(() => {

    }, [])
}